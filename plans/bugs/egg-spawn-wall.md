bug:
- i saw an enemy spawn (likely the egg too) outside the boundaries of the map, or likely in a wall

FIRST fix the above bug, likely where the DarkTower is allowed to spawn eggs, make sure they are within the map and don't spawn on things that are "solid" or whatever like walls, fixture buildings, the player, an enemy etc. if it fails with "solid" then simply pick a different random spot again, if it fails like 3 times, just stop and don't spawn the egg.

another change to implement while we fix this bug:
- we should just remove walls surrounding the current level 1 for now, and reserve them for like barriers or blockages the network can't be built on. and then similar we'll add visual tweak variations like water, or stones, or whatever, but that can be later. 
